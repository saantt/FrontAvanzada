export interface Categoria {
  id?: string;      // Opcional, usado al editar/eliminar
  nombre: string;
  icono: string;    // Puede ser un emoji o nombre de ícono si usas alguna librería como FontAwesome
}
